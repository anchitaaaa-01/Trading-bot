I need a complete Python trading bot for Binance Futures Testnet that meets the following exact requirements. Please generate the full project with all files.

## Project Requirements:

Create a trading bot with these capabilities:
1. Place MARKET and LIMIT orders on Binance Futures Testnet (USDT-M)
2. Support BUY and SELL sides
3. CLI interface using argparse with these arguments:
   - --symbol (e.g., BTCUSDT)
   - --side (BUY/SELL)
   - --order-type (MARKET/LIMIT)
   - --quantity (float)
   - --price (float, required only for LIMIT orders)
4. Print order summary, response details (orderId, status, executedQty, avgPrice), and success/failure message
5. Structured code with separate layers:
   - client.py (API wrapper for Binance Futures)
   - orders.py (order placement logic)
   - validators.py (input validation)
   - logging_config.py (logging setup)
   - cli.py (main entry point)
6. Log all API requests, responses, and errors to a log file
7. Comprehensive exception handling (invalid input, API errors, network failures)
8. Use either python-binance library or direct REST calls to https://testnet.binancefuture.com

## Bonus Feature (include this):
Add a lightweight UI using Rich library (console-based interactive menu with colorful output) that allows users to place orders without command-line arguments, while still supporting CLI arguments.

## Deliverables to generate:

1. Complete source code with all files in a trading_bot/ folder structure
2. README.md with:
   - Setup steps (including virtual environment)
   - How to get Binance Testnet API credentials
   - How to run with examples (both CLI and interactive mode)
   - Assumptions made
3. requirements.txt
4. Sample log files showing:
   - One MARKET order execution
   - One LIMIT order execution

## Additional Requirements:

- Use python-dotenv for API key management
- Use Rich library for beautiful console output in interactive mode
- Include retry logic with exponential backoff for network failures
- Validate quantity against minimum notional and step size (you can assume typical BTCUSDT values: min_qty=0.001, step_size=0.001)
- All API calls should be async? (No, use synchronous requests for simplicity)
- Log files should rotate after 5MB
- Include a .env.example file

## Code Quality Expectations:

- Type hints throughout
- Docstrings for all public functions
- Constants in UPPER_CASE
- No hardcoded credentials
- Error messages should be user-friendly but detailed in logs

Please generate the complete project as separate code blocks for each file, with clear file paths indicated.